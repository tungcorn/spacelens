# release-pr.yml — RETIRED
#
# The `release/next` PR mechanism has been replaced by direct version-bump
# commits on main via the `prepare` job in release.yml.
# See docs/RELEASING.md for the current automated release flow.
#
# This file is kept as a stub so that any external references to the
# workflow filename do not produce 404 errors. It performs no actions.

name: Release PR (retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    name: Retired — see release.yml prepare job
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: read
    steps:
      - name: This workflow is retired
        run: |
          echo "release-pr.yml is retired."
          echo "Automated patch releases are now prepared by the 'prepare' job in release.yml."
          echo "See docs/RELEASING.md for the current release flow."
