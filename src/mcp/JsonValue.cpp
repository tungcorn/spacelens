#include "mcp/JsonValue.hpp"

// Legacy source lists compiled this translation unit directly instead of
// linking spacelens_core. Keep that path self-contained while the repository's
// CMake targets compile core/JsonValue.cpp only once through spacelens_core.
#include "core/JsonValue.cpp"
