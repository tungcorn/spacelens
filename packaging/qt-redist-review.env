# SpaceLens Qt redistribution review record.
# Parsed by scripts/verify-qt-redist-review.ps1.
# Presence of this file is NOT approval.
# review_status=PASS is valid only with linkage=shared, a matching
# qt_version, and source_availability=READY.
# An assistant must not set review_status=PASS.

review_status=PENDING
qt_version=6.8.3
linkage=shared
source_availability=MISSING
